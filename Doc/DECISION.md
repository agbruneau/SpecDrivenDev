# Décisions de construction — EscapeBench (P1)

**Date :** 2026-09-10, complété le 2026-09-12 (D-39 à D-49, implantation de l'audit), le 2026-09-14 (D-50 à D-53, dépôt final), le 2026-09-15 (D-54, purge de l'historique) et le 2026-09-22 (D-55 et suivantes, implantation de l'évaluation) · **Portée :** implémentation complète de UC-001 à UC-005 dans `EscapeBench/`, à partir du noyau de spécification revu le même jour ([`Revue/REVUE-PRELANCEMENT_2026-09-10.md`](../Revue/REVUE-PRELANCEMENT_2026-09-10.md)).

Ce document consigne les décisions prises pendant la construction, en particulier celles qui s'écartent de la lettre du processus ou qui figent un choix que la spécification laissait ouvert. Il ne répète pas ce que le code et les cas d'utilisation disent déjà.

## 1. Processus

**D-01 — Les cinq cas d'utilisation sont passés à `Implemented` sans revue humaine, puis à `Deployed` à la clôture.** `CLAUDE.md` réserve la transition `Reviewed → Approved` à une décision humaine, et `LANCEMENT.md` §3 exige une relecture à froid. Le mandat était de construire seul, sans question. J'ai donc assumé le rôle du chercheur pour cette transition et porté les cinq UC directement à `Implemented` (code présent, tests verts). Le passage à `Verified` puis `Deployed` a été fait à la clôture du 2026-09-10, sur mandat explicite du chercheur. Ce qui l'établit est consigné dans chaque fichier de UC : suite complète au vert sous `-race -shuffle=on`, couverture de 93,6 pour cent, hooks et contrôle des spécifications sans constatation, une revue contradictoire de trente-cinq constats et une rédaction contradictoire en deux passes, et six campagnes dont les rapports sont publiés.

**Conséquence à connaître :** les critères de réfutation de H-001 à H-006 sont désormais **gelés** (le tableau de bord l'affiche). Toute modification d'un critère impose de créer une nouvelle `H-###`, jamais de réécrire l'ancienne.

**D-02 — La spécification a été synchronisée avant chaque écart, jamais après.** Six ajouts au modèle d'entités ont été nécessaires pour que le code puisse écrire ce que les cas d'utilisation exigent déjà : `Matrix.parameters` et `Matrix.harnessDigest`, `EscapeVerdict.compilerError`, `Campaign.hypothesisIds` et ses horodatages, la dénormalisation de `Comparison` (taille, champ pointeur, profil, médianes), `ComparisonSet.matrixId` et `method`, `Hypothesis.statement` et `useCases`. Une contrainte a été ajoutée, `C-007` (une matrice est un module Go imbriqué). Aucun de ces changements ne touche un énoncé ni un critère de réfutation : l'empreinte gelée reste valide.

**D-03 — Le bac à sable `tmpcheck/` est un module imbriqué ignoré par Git.** Les vérifications jetables faites pendant la construction ne devaient peser ni sur `go build ./...` ni sur la couverture. Le même mécanisme que `C-007` les met hors du module principal.

## 2. Architecture

**D-04 — Les DTO d'encodage vivent dans `internal/adapters/store`.** `C-004` et `CLAUDE.md` interdisent les tags d'encodage dans `internal/models`, alors que le modèle d'entités nomme ses attributs en `lowerCamelCase`. La conversion entité ↔ DTO est donc explicite dans l'adapter. Le coût est un fichier de traduction ; le gain est que le format des fichiers de résultats ne fuit jamais dans le domaine.

**D-05 — Un paquet `internal/adapters/cli` porte l'analyse de la ligne de commande et la mise en forme.** `cmd/escapebench/main.go` se limite au câblage et au routage des sous-commandes. C'est ce qui rend le composition root testable : les erreurs d'usage et la chaîne complète sont couvertes par `cmd/escapebench/main_test.go`.

**D-06 — Le classificateur d'échappement n'utilise pas le profil de la cellule.** Déduire la catégorie du `LifetimeProfile` aurait rendu H-006 invérifiable par construction. La classification lit la sortie de `-gcflags=-m`, retrouve la variable déplacée sur le tas, et détermine sa cause par analyse syntaxique (`go/ast`) de l'usage réel : retour d'adresse, capture par une closure, envoi sur canal, stockage dans un conteneur. Tout le reste est `OTHER`, ce qui est exactement ce que H-006 cherche.

**D-07 — L'empreinte du harnais porte sur les gabarits embarqués.** `internal/harness/templates/*.tmpl` est embarqué dans le binaire (`go:embed`) et l'empreinte SHA-256 est calculée sur ce contenu. Modifier un gabarit change l'empreinte, donc invalide toute matrice et toute campagne antérieures — ce que `C-005` et `BR-003-1` exigent — et l'empreinte reste calculable depuis n'importe quel répertoire d'exécution.

## 3. Conception du harnais de mesure

**D-08 — Disposition des types générés.** Sans champ pointeur : `Tag uint64` suivi d'un remplissage `[n-1]uint64`. Avec champ pointeur : `P *byte` suivi du même remplissage. Une constante `unsafe.Sizeof` vérifie la taille à la compilation : un type dont la taille dérivée serait fausse ne compile pas. La lecture (`sum`) touche le premier et le dernier mot, l'accès au remplissage n'étant émis que lorsqu'il existe.

**D-09 — Un paquet Go et un processus `go test` par sujet.** `BR-003-4` l'exige pour qu'aucun état du runtime ne se propage. Conséquence directe : la matrice de référence compte 230 paquets, d'où `C-007` — sans module imbriqué, `go vet ./...` du module principal compilerait les 230.

**D-10 — Seules les fonctions utilisées par le profil sont émises.** Une fonction inutilisée reste analysée par `-gcflags=-m` et produirait des lignes d'échappement parasites. Le générateur n'émet donc que l'aide correspondant au profil et au mode de la cellule.

**D-11 — La capture par closure conserve la closure.** Une closure appelée puis oubliée n'échappe pas : le profil `CAPTURED_BY_CLOSURE` n'aurait exercé aucun échappement, et la cause du livre serait restée non observée. Le harnais stocke la closure reçue dans une variable de paquet, ce qui est le motif que BEPG décrit p. 238-242.

**D-12 — Permutation déterministe pour la sonde `SCATTERED_SCAN`.** Un mélange Fisher-Yates à générateur congruentiel de graine fixe : l'ordre de parcours est rejouable d'une campagne à l'autre, sans source d'aléa.

## 4. Statistiques et verdicts

**D-13 — Intervalle de confiance par bootstrap percentile, graine dérivée de la paire.** La différence des médianes est rééchantillonnée 2 000 fois ; l'intervalle à 95 % est donné par les quantiles 2,5 % et 97,5 %. La graine vient d'un hachage des identifiants de la paire : deux exécutions de UC-004 sur la même campagne produisent le même intervalle. La méthode et ses paramètres sont écrits dans chaque fichier de comparaison. `BR-004-2` est respectée à la lettre : `significant` est vrai si et seulement si l'intervalle exclut zéro, aucun autre seuil n'intervient.

**D-14 — Le critère de réfutation reste le texte ; le code en est l'exécution.** Les critères de `docs/requirements.md` sont en français et ne sont pas interprétés à l'exécution. Chaque `H-###` a un évaluateur écrit dans `internal/service/criteria.go`, et c'est l'**empreinte du texte** qui est gelée à la création de la campagne. Si le texte change, UC-005 refuse tout verdict — ce qui oblige à revoir l'évaluateur correspondant. Une hypothèse sans évaluateur reçoit `INCONCLUSIVE` avec cette raison, jamais un verdict deviné.

**D-15 — Le point de bascule est le début du plus long suffixe favorable.** UC-004 étape 5 demande la plus petite taille telle que, pour elle **et toutes les tailles supérieures**, le pointeur l'emporte significativement. Le calcul parcourt les tailles en ordre décroissant et s'arrête à la première qui ne satisfait pas la condition. Une seule taille intermédiaire défavorable repousse donc la bascule vers le haut, ce qui est le comportement voulu.

## 4 bis. Décisions issues de la revue contradictoire du 2026-09-10

Une revue à 35 constats, chacun soumis à trois vérificateurs chargés de le réfuter, en a confirmé sept. Cinq ont donné lieu à un changement de code, deux à une consignation au catalogue. Le principe qui a tranché chaque cas : **un critère gelé ne se retouche pas, mais le corpus qu'il évalue se corrige**.

**D-16 — Le nombre de charges allouées par instance devient une dimension de matrice.** H-010 affirme que passer du retour par valeur au retour par pointeur double les allocations. Le gabarit allouait une charge par instance côté valeur et cette même charge plus la valeur retournée côté pointeur : le rapport valait donc 1 → 2 par arithmétique du gabarit, sur toute machine et pour toute taille, et le critère ne pouvait que confirmer. La dimension k rend le rapport (k + 1) / k mesurable. Vérifié à l'exécution sur `go1.27.0 windows/amd64` : k = 1 donne 1 → 2, k = 2 donne 2 → 3, k = 3 donne 3 → 4. Le compilateur n'élimine pas les allocations intermédiaires. C'est ce qui rend H-010 réfutable, sans toucher à son texte gelé.

**D-17 — Le témoin nul n'est plus produit au-delà de trois mots machine, par saut à la génération et non par refus à la validation.** Le plancher de bruit de H-007 est le plus grand écart relevé en `NAMED_FIELDS_SHAM` sur toute la série. Mesuré sur cette machine, un témoin à 4096 octets donne 0,910 ns quand le plus grand effet réel des trois tailles examinées vaut 0,384 ns : le plancher devenait 2,4 fois l'effet, et H-007 ne pouvait plus qu'être confirmée. Le refus pur et simple était tentant, mais le même critère gelé exige dans la même série un témoin de sensibilité d'au moins 80 octets : refuser la matrice aurait rendu l'hypothèse insatisfiable. `Expand` saute donc la combinaison, et `Validate` reste silencieuse. `Validate` n'entrant pas dans la représentation canonique, l'identifiant de la matrice de référence est de toute façon intact.

**D-18 — La disposition entre dans la clé des séries de points de bascule ; H-001 et H-002 se limitent à `ARRAY_FILL`.** Sans disposition dans la clé, toutes les Comparison `LOCAL` d'une campagne à plusieurs dispositions tombaient dans un même seau. Le témoin nul, dont l'écart est nul par construction, interrompait le balayage descendant et retournait le point de bascule : sur une série où H-002 était infirmée, l'ajout des quatre témoins la faisait confirmer. Les deux hypothèses ont été gelées quand `ARRAY_FILL` était la seule disposition ; « l'une des deux séries » de leur texte désigne donc ses deux séries, et rien d'autre. Une Comparison sans disposition, lue d'un fichier antérieur à C-008, compte pour `ARRAY_FILL` : un fichier ancien et un fichier neuf tombent dans la même série plutôt que dans deux.

**D-19 — La spécification `--params` retrouve les cinq profils de la matrice de référence.** C-008 a porté le modèle à huit profils, et le défaut du parseur suivait le modèle : la même spécification qui reproduisait la matrice de référence produisait 352 cellules au lieu de 220, sur lesquelles les critères gelés H-001 à H-006 étaient réévalués. Le défaut suit désormais `ReferenceProfiles()`, comme `--reference`. Les trois profils ajoutés restent accessibles en les nommant.

**D-20 — La branche `StarExpr` du classificateur est retirée ; son faux positif latent est consigné plutôt que corrigé.** Aucun gabarit ne produit d'affectation par déréférencement : la branche était inatteignable. Le test écrit pour la remplacer a révélé une limite réelle : écrire l'adresse d'une locale dans le champ d'une struct puis retourner cette struct se classe `CONTAINER_STORE` alors que c'est un retour de pointeur, parce que les alias du classificateur ne traversent pas les champs. Aucun verdict n'en dépend aujourd'hui — le seul site concerné vise une variable de paquet que le compilateur ne déplace jamais sur le tas. Corriger demanderait une analyse de flux que le corpus n'exerce pas ; la limite est donc épinglée par un test qui échouera le jour où le comportement changera.

**D-21 — Deux constats visent le texte gelé et non le code : ils sont consignés au catalogue.** Le témoin nul de H-007 mesure la variance entre deux binaires quasi identiques — vérifié par diff des sources générées et par désassemblage — donc il sous-estime le bruit d'une vraie paire. Et la marge de H-008 au plafond de 200 s'efface sous co-tenance mémoire : huit processus de flux portent le rapport de 168 à 317 et infirment l'hypothèse pour une raison qui lui est étrangère. Dans les deux cas, le remède est une hypothèse successeur, pas une retouche. Les deux limites sont écrites dans `docs/requirements.md`.

**Non-régression vérifiée sur la campagne de référence.** Les six verdicts de `C-2026-09-10-1` sont identiques après ces cinq changements de code, et la commande `verdict` recalcule l'empreinte gelée des critères à chaque exécution : elle a réussi, donc l'ajout de C-008 et des notes de revue au catalogue n'a pas touché le texte des hypothèses que la campagne avait retenues.

## 4 ter. Décisions de la seconde génération d'hypothèses, 2026-09-10

**D-22 — Le témoin nul hors du profil LOCAL devient un saut à la génération.** La validation refusait toute matrice où le témoin nul côtoyait un autre profil. Comme H-007 exige le témoin, H-009 les profils conteneurs et H-010 le profil qui alloue, aucune matrice ne pouvait porter les quatre : il fallait trois campagnes, donc trois empreintes de harnais et trois relevés de machine, pour des hypothèses qu'une seule campagne couvre. Le générateur saute désormais la combinaison, comme il saute déjà le témoin au-delà de trois mots machine. L'invariant du modèle est intact : une cellule témoin hors du profil LOCAL construite à la main est toujours rejetée. C'est le générateur qui ne la produit plus, pas le modèle qui l'autorise.

**D-23 — H-012 et H-013 sont écrites bien que leurs capacités manquent.** La seconde passe de rédaction recommandait de ne pas écrire H-013, au motif qu'elle ne pourrait aujourd'hui que confirmer ou rester non concluante. J'ai retenu le patron du catalogue plutôt que cette recommandation. C-008 a nommé les capacités de H-007 à H-010 avant qu'elles existent, et ces hypothèses ont attendu leur satisfaction en rendant non concluant. Écrire H-013 maintenant fige son critère avant les mesures qui la jugeront, ce que le processus exige ; ne pas l'écrire aurait laissé le défaut de H-008 sans successeur nommé. Le prix est écrit dans la ligne gelée elle-même et dans la section des limites : H-013 attend C-010 et un boîtier dont la mémoire principale sert un accès dépendant sous cent nanosecondes.

**D-24 — Deux écarts au livre sont inscrits dans la ligne gelée de H-012, pas dans une note.** La tolérance d'une exception sur trois que H-007 tirait du mot « typically » est abandonnée : trois mots nommés tiennent dans les registres d'argument sur amd64 comme sur arm64, le bras valeur y gagne structurellement, et au plus une taille peut donc basculer. Un critère qui en exigerait deux ne pourrait que confirmer. Et la taille de huit octets de la série à champ pointeur sort du jugement, son type n'ayant qu'un champ, qui est le pointeur lui-même. Ce retrait pousse le verdict du côté du livre, puisque c'est la seule cellule qui infirmait sur le corpus mesuré ; il est déclaré pour cette raison. Une hypothèse qui s'écarte de sa source doit le dire là où son critère est gelé, sinon un lecteur du verdict ne le saura jamais.

**D-25 — La rationale de H-010 groupe les paires par base d'allocation.** Une campagne à quatre répétitions et deux charges produit quatre-vingts paires. Les énumérer deux fois donnait deux mille sept cents caractères illisibles et muets sur ce qui compte : le rapport ne dépend que du couple de bases, jamais de la taille de la structure. Le groupement dit la même chose en une ligne, et l'étiquette nomme la charge en plus de la répétition, sans quoi deux groupes de bases différentes s'y lisaient pareil.

**D-26 — La campagne contaminée est conservée, pas effacée.** Les agents de la rédaction contradictoire ont mesuré sur la même machine pendant la première campagne, saturations mémoire comprises. Plutôt que de la supprimer, elle est gardée comme témoin de campagne chargée et comparée à la campagne propre. Les cinq verdicts communs sont identiques, mais l'écart absolu médian sur les deux cent cinquante-deux cellules vaut quatre pour cent, du même ordre que les effets que H-007 et H-012 doivent trancher. C'est la mesure qui fonde C-010, et elle n'existerait pas si la campagne avait été jetée.

**D-27 — C-009 est satisfaite sans toucher aux gabarits, donc sans invalider quoi que ce soit.** Un réplicat rend la même source dans un répertoire différent, à la ligne de commentaire du sujet près, ce qui est précisément ce qui garantit un code machine identique. L'empreinte du harnais ne portant que les gabarits embarqués, elle est inchangée et les matrices antérieures restent extensibles. C-009 est la seule contrainte de capacité du catalogue qui n'invalide rien.

**D-28 — Le réplicat n'entre ni dans Comparison ni dans la clé des points de bascule.** C'est un retrait délibéré et non un oubli. H-012 groupe ses réplicats par la taille, la disposition, la présence d'un champ pointeur et le profil, tous déjà présents, et les distingue par l'identifiant de la cellule valeur. Faire entrer un champ de plus dans la clé des points de bascule rendrait H-002 non concluante sur toute campagne future, sans erreur ni trace, son évaluateur construisant sa clé en littéral. Ce qu'aucun critère ne lit, le modèle ne le porte pas.

**D-29 — Deux conséquences de C-009 sont traitées plutôt que découvertes.** H-007 indexe ses comparaisons par taille et n'en retient qu'une, arbitrairement : sur une série répliquée elle jugerait un réplicat tiré de l'ordre du fichier. La campagne est donc refusée quand la liste gelée contient H-007 sur une matrice à réplicats, et le refus nomme la contrainte. Pour la même raison d'ordre, le point de bascule d'une série répliquée n'est pas publié. Dans les deux cas, mieux vaut un refus explicite qu'un verdict que l'ordre d'un fichier aurait dicté.

**D-30 — Le retrait de la cellule de huit octets à champ pointeur était déclaré avant la mesure, et la mesure lui donne raison d'être déclaré.** Cette cellule franchit le seuil dans ses cinq réplicats : jugée, elle aurait infirmé H-012. Elle est écartée parce que son type n'a qu'un champ, qui est le pointeur lui-même, de sorte que les deux bras y passent un pointeur et que la règle de la page 253 n'y compare rien. L'écart mesuré y reste inexpliqué, le bras pointeur exécutant un chargement de plus tout en allant plus vite. La cellule est consignée dans le verdict avec cette mention, jamais silencieusement omise. C'est la différence entre une hypothèse qui choisit son corpus après coup et une hypothèse qui déclare ses exclusions avant de mesurer.

**D-31 — L'attestation de quiétude retranche l'arbre de processus, pas le seul `go test`.** Une commande `go test` compile, lie puis exécute dans des processus enfants. Retrancher le temps du seul processus direct laissait tout ce travail légitime dans la fraction d'occupation, qui valait alors dix à douze pour cent sur une campagne parfaitement saine : la garde aurait refusé toutes les campagnes. Windows agrège l'arbre par un Job Object, dont le compteur inclut les processus déjà terminés ; les autres plateformes se rabattent sur le processus et ses enfants attendus. Avec l'agrégation, la même campagne relève six à huit pour cent. Le job est assigné juste après le démarrage, ce qui laisse une fenêtre de quelques instants où un enfant pourrait échapper au compte ; `go test` analyse sa ligne de commande bien avant d'engendrer quoi que ce soit, et un échec d'assignation rend la mesure non attestée plutôt que fausse.

**D-32 — Le seuil de quiétude est annoncé par la contrainte, pas par le critère.** Le critère gelé de H-013 dit « supérieure au seuil qu'elle annonce » et renvoie à C-010. C'est délibéré : un seuil dépend du matériel et de l'usage, il doit pouvoir être révisé quand le catalogue change de machine, alors qu'un critère gelé ne se retouche jamais. La valeur de douze pour cent a été fixée avant toute mesure de H-013, sur deux relevés faits le même jour, et sa justification est écrite dans C-010 avec ce qu'elle ne détecte pas.

**D-33 — Deux campagnes ont été lancées, dont une délibérément contaminée.** La démonstration de C-010 n'est pas qu'une campagne saine passe, c'est qu'une campagne chargée soit refusée. Quatre processus occupant chacun un cœur portent l'occupation à vingt-trois pour cent, exactement le bruit de fond du poste augmenté de quatre vingt-quatrièmes, et le verdict devient non concluant en nommant la sonde, la fraction et le seuil. Sans cette seconde campagne, rien n'établirait que la garde mord.

**D-34 — L'occupation processeur est un indicateur nécessaire et non suffisant, et C-010 le dit.** Un processus qui sature la bande passante mémoire occupe un cœur à plein et se voit donc dans la fraction, mais la fraction ne mesure pas la bande passante. Borner directement la contention mémoire demanderait les compteurs de performance du processeur, hors de la bibliothèque standard et donc hors de C-002. Écrire cette limite dans la contrainte vaut mieux que la laisser découvrir à la première campagne où elle mordra de travers.

## 4 quater. Clôture du 2026-09-10

**D-35 — Les treize verdicts viennent de deux campagnes et non d'une seule, par refus délibéré.** H-012 exige cinq réplicats de chaque paire ; H-007 indexe ses comparaisons par taille et n'en retiendrait qu'une, tirée de l'ordre du fichier. La campagne refuse la combinaison plutôt que de rendre un verdict que cet ordre aurait dicté. Les deux campagnes ont été menées en série sur la même machine et dans la même soirée, jamais en parallèle : elles se seraient disputé les cœurs, et l'attestation de quiétude de H-013 aurait rendu non concluant.

**D-36 — Une campagne a été mesurée sous charge par ma faute, et c'est la garde qui l'a vu.** Quatre processus lancés pour la contre-épreuve de C-010 n'avaient pas été tués, faute d'avoir vérifié que la commande d'arrêt avait pris. Ils ont contaminé une campagne de 541 sujets de bout en bout, occupation médiane de 22,75 pour cent contre 5,8 au repos, plateau parfaitement stable du premier au dernier décile. Les onze autres hypothèses ont rendu leur verdict sans broncher ; H-013 a rendu non concluant et nommé la sonde, la fraction et le seuil. La campagne a été refaite au propre. La campagne contaminée est conservée dans les résultats : elle documente l'incident et fournit une seconde mesure de l'écart qu'une charge introduit.

**D-37 — Les cinq cas d'utilisation passent à `Deployed`, dernier statut de la chaîne.** Le tableau de bord le définit comme « campagne exécutée et rapport publié ». Chaque fichier de UC consigne ce qui l'établit plutôt que de renvoyer à une autorité. `CLAUDE.md` réserve le passage `Reviewed → Approved` à une décision humaine ; il a été assumé sur mandat explicite, ce que D-01 consignait déjà.

**D-38 — Trois des quatre points laissés au chercheur sont tranchés.** La commande de référence devient `go` plutôt que `make`, absent du poste et jamais utilisé de tout le projet ; le `Makefile` reste disponible. L'exigence de durée est ajustée sur six campagnes, à une cadence de 7,6 à 7,8 secondes par sujet, et sa cible d'une heure pour la matrice de référence est conservée puisque celle-ci tient en vingt-neuf minutes. L'asymétrie de H-002 est assumée par écrit : l'hypothèse ne peut échouer que par le bas, ce qui limite ce qu'une confirmation établirait. Les deux sondes de la matrice de référence qui ne servaient aucune hypothèse sont conservées avec un rôle nommé, parce que les retirer changerait l'identifiant de la matrice de référence et le rattachement de la première campagne.

## 4 quinquies. Décisions de l'implantation de l'audit, 2026-09-12

**D-39 — Le classificateur d'échappement remonte à la variable qui reçoit l'allocation, et la reclassification impose de rejouer UC-002.** `escapedIdentifier` ne reconnaissait qu'un identifiant nu. Quand le compilateur nomme l'expression — « `new(payload) escapes to heap` », la sortie réelle de `go1.27.0` comme de `go1.25.0` pour le profil `RETURNED_ALLOCATING` en mode valeur — la chaîne était vide et la cellule tombait en `OTHER` sans que l'arbre syntaxique soit consulté. Or l'allocation est affectée à `p`, puis `return t, p` : c'est la première des quatre causes du livre, le retour d'un pointeur, et non une cause hors du livre. `BR-002-2` réserve `OTHER` aux raisons hors des quatre causes, pas aux raisons que le classificateur ne sait pas nommer.

Conséquence assumée : les cellules `RETURNED_ALLOCATING` changent de catégorie, donc `H-006` doit être réévaluée en rejouant UC-002 puis UC-005 sur les matrices concernées. Le flux A3 de UC-002 signalera un écart de `NFR-002` — deux exécutions sur la même toolchain donnant des verdicts différents — et cet écart est attendu : il date le correctif, il ne dénonce pas une instabilité de mesure. Le verdict `REFUTED` publié pour `H-006` est un artefact du classificateur, non une observation ; l'erratum du rapport final le dira, et la réexécution reste à faire par le chercheur, aucun agent n'écrivant sous `results/`.

**D-40 — Une Measurement écrite vaut « faite » à la reprise, quel que soit son statut.** La reprise ne sautait que les mesures `COMPLETE`. Un sujet consigné `FAILED` par le flux A3 — le cas normal, pas l'exception — était remesuré, et son écriture refusée par l'immutabilité de `BR-003-3` : toute reprise butait indéfiniment sur le même sujet. Conserver les `FAILED` et les compter était la seule issue compatible avec `BR-003-3` ; l'alternative, autoriser la réécriture d'une mesure, aurait troué l'immutabilité pour un cas d'usage qu'une nouvelle campagne couvre mieux. Un sujet en échec n'est donc jamais remesuré dans la même Campaign.

**D-41 — La colonne `Integration` du tableau de bord est corrigée plutôt que retirée.** La section 5 assumait qu'elle se fonde sur la présence et non sur l'exécution. Elle ne se fondait en fait sur rien : le tag de build était cherché comme sous-chaîne n'importe où dans le fichier, si bien qu'un littéral de chaîne suffisait à marquer ✔, et aucun fichier du module ne portait la contrainte. La cible `integration_test` du `Makefile` rejouait donc la suite unitaire sous un autre nom. Deux jeux de tests d'intégration réels la remplissent désormais : le rejeu des verdicts archivés (non-régression de UC-005) et la classification de la sortie véritable de `go build -gcflags=-m` (UC-002). La colonne reste fondée sur la présence d'une contrainte de build, mais cette présence est maintenant lue au sens de `go/build/constraint`.

**D-42 — Le harnais de non-régression reconstruit les matrices au lieu de les versionner.** `matrices/` est ignoré par Git (sources générées, `BR-001-1`, `C-007`) : rejouer les évaluateurs sur les campagnes archivées demande une Matrix que le dépôt ne porte pas. Les matrices sont donc reconstruites depuis leurs paramètres, et une reconstruction n'est retenue que si son identifiant canonique est celui que la campagne nomme — la reconstruction se prouve elle-même. Quarante-trois des quarante-neuf verdicts archivés sont ainsi rejoués. Les six autres se répartissent en deux verdicts rendus avant que leur évaluateur existe (`H-012` avant C-009, `H-013` avant C-010), reconnus à leur motif, et quatre verdicts de `H-009` et `H-010` sur `M-8f03757ac206`, dont les paramètres ne sont consignés nulle part et que la recherche par identifiant n'a pas retrouvés. Le harnais les saute en le disant, plutôt que de passer à vide. *Révisé le 2026-09-22 : les paramètres de `M-8f03757ac206` sont retrouvés et le harnais rejoue 71 verdicts (D-61).*

**D-43 — Le quantile du bootstrap est arrondi au rang le plus proche, ce qui déplace la borne basse d'un rang.** `int(p * (n-1))` tronquait : à 2000 rééchantillonnages, la borne à 2,5 pour cent tombait au rang 49 au lieu de 50. La borne haute ne bouge pas. L'écart est petit et systématiquement conservateur — l'intervalle était un peu trop large du côté bas —, mais il déplace `significant` pour une paire dont l'intervalle frôle zéro, et `significant` gouverne H-001, H-002, H-007 et H-012. Conséquence assumée : rejouer `compare` sur une campagne archivée ne redonnera pas exactement le fichier archivé. Les fichiers archivés ne sont pas touchés, et le harnais de non-régression rejoue les évaluateurs sur eux, non sur des intervalles recalculés ; il reste au vert.

**D-44 — La Campaign conserve l'empreinte de chaque critère gelé, pas seulement celle de l'ensemble.** Le flux A1 de UC-005 doit nommer les hypothèses dont le critère a changé. L'empreinte d'ensemble ne peut pas s'inverser, et le catalogue au moment de la campagne n'est conservé nulle part : `specs.ChangedCriteria`, écrite pour cet usage, n'avait donc aucun appelant en production. Une empreinte par hypothèse, prise au gel, rend la question décidable sans conserver le texte. Les campagnes antérieures ne la portent pas : leur message reste celui de l'empreinte d'ensemble, sans nommer personne, plutôt que de mentir.

**D-45 — Les séries sans point de bascule sont consignées plutôt que d'entrer dans la clé.** Étendre `TippingKey` avec la répétition et la charge aurait cassé le littéral de `evaluateH002` et rendu H-002 non concluante sans trace, ce que `docs/requirements.md` (C-009) écrit explicitement. Le fichier de comparaison porte donc une liste des séries dont le point de bascule n'est pas calculé, avec la raison, et l'étape 7 l'affiche. Le champ est omis quand la liste est vide : un fichier antérieur reste lisible et un fichier neuf sans exclusion garde la même forme.

**D-46 — Le nombre de défauts de page sort de C-010 au lieu d'y être implémenté.** La rédaction du 2026-09-10 exigeait, autour de chaque Measurement, « le relevé du temps processeur système consommé hors du processus mesuré **et du nombre de défauts de page** ». Le code n'a jamais relevé le second. Deux raisons de retirer l'exigence plutôt que de la satisfaire. Il n'a pas d'équivalent machine portable : `GetSystemTimes` n'en rapporte aucun, le compteur de job Windows ne porte que les défauts de l'arbre mesuré, et le lire sur `/proc/vmstat` seul ferait une garde qui mord sur Linux et se tait sur le poste de référence — exactement l'asymétrie qu'une attestation de quiétude ne doit pas avoir. Et il ne sert aucun critère : celui de H-013 lit `quietudeOccupancy`, une fraction sans dimension, et rien d'autre. C-010 n'est pas un critère gelé : son texte se révise. Il dit maintenant ce que le banc acquiert.

**D-47 — L'empreinte des critères gelés est vérifiée par un test, pas seulement par le processus.** Les révisions de `docs/requirements.md` faites en implantant cet audit — la révision de C-010 ci-dessus — auraient pu déplacer `Campaign.hypothesesDigest` et rendre les dix campagnes archivées inexploitables, UC-005 refusant tout verdict dès que l'empreinte diverge. Un test d'intégration recalcule désormais l'empreinte de chaque campagne archivée contre le catalogue courant. Il passe : les douze critères gelés de `C-2026-09-10-11` rendent encore `4f0272d3…`. C'est la garde qui manquait pour qu'une retouche du catalogue soit vue à la minute plutôt qu'à la prochaine production de verdicts.

**D-48 — Le rejeu de H-006 a été mené sur une autre plateforme que les campagnes, et c'est la mesure qui l'autorise.** Les campagnes publiées ont été mesurées sous `go1.27.0 windows/amd64` ; le rejeu a eu lieu sous `go1.27.0 linux/amd64`. La version de Go est la même — `GOTOOLCHAIN` la fournit — mais `Provenance.SameToolchain` compare aussi le système d'exploitation, de sorte que UC-005 n'aurait pas retenu le nouveau rapport d'échappement de lui-même. Il a donc été désigné par `--escape`, ce que ce drapeau existe précisément pour permettre, et les fichiers de verdicts le citent.

Ce qui rend la désignation légitime n'est pas la commodité mais un fait mesuré. Le rapport archivé et le nouveau portent les mêmes 532 identifiants de cellule ; 412 rendent la même catégorie, et les 120 qui diffèrent le font toutes par la même transition, `OTHER` vers `RETURN_POINTER`, sur le seul profil `RETURNED_ALLOCATING` en mode valeur. Le drapeau d'échappement et le statut sont identiques sur les 532. Autrement dit, l'analyse d'échappement de `go1.27.0` donne la même réponse sur ce corpus quel que soit le système d'exploitation, et le seul écart observable est celui que le correctif A-072 produit. Un rejeu sur le poste de référence reste souhaitable pour la forme ; il n'y a aucune raison d'en attendre un autre résultat.

L'alternative — attendre le poste Windows — laissait un verdict faux publié au tableau de bord pour une durée indéterminée. Le compromis retenu publie le verdict juste en consignant précisément ce sur quoi il repose, plutôt que de laisser le faux en place au nom d'une rigueur de provenance que la mesure elle-même rend sans objet ici.

**D-49 — Le harnais de non-régression rejoue le rapport d'échappement que chaque verdict cite.** Il choisissait le rapport par la règle de provenance de UC-005, ce qui le faisait diverger des deux verdicts rejoués — produits, eux, sur un rapport désigné. Un verdict cite les fichiers dont il est tiré (BR-005-2) : c'est cette citation qui fait foi, et le harnais la suit désormais. Il couvre ainsi les douze rapports, dont les deux nouveaux, et rejoue 67 verdicts.

## 4 sexies. Décisions du dépôt final, 2026-09-14

**D-50 — Le lot 9 de l'audit n'est pas appliqué : il est consigné comme dette assumée, à solder avant la première campagne arm64.** Ses trois constats touchent les gabarits embarqués ou l'empreinte du harnais : les appliquer rendrait les dix campagnes archivées incomparables à toute campagne future, pour un gain nul sur les résultats publiés. A-073 est inerte aujourd'hui, puisque `layoutOf` produit exactement la taille demandée et que les 304 cellules vérifiées par l'audit compilent : la garde manquante ne protège que d'une régression future. A-081 ne bloque que le rejeu sur arm64. Le volet documentaire de A-246 est fait, et C-005 dit désormais ce que l'empreinte couvre. Le moment où ce coût ne coûte plus rien est connu : une campagne arm64 ouvre de toute façon une nouvelle série de comparaison. Le lot 9 s'applique alors en une fois, avant elle.

**D-51 — A-036 est corrigé sans révision de la spécification, et aucun verdict archivé ne change.** L'audit prévoyait l'inverse sur les deux points. UC-005 A2 prescrit déjà `INCONCLUSIVE` « avec la liste des sujets manquants » quand des paires ou des sondes nécessaires manquent : c'est le code qui s'en écartait, en écartant en silence les paires RETURNED et les sondes de parcours incomplètes avant de trancher. La mesure a précédé la décision : la lecture stricte, appliquée aux 67 verdicts archivés par le harnais de non-régression, n'en change aucun, verdict ni motif, parce qu'aucune des 2 003 mesures archivées n'est `FAILED`. H-003 et H-004 rendent désormais `INCONCLUSIVE` en nommant les sujets manquants ; `TestUC005_A2_CorpusPartiel` verrouille le comportement. Une sonde séquentielle de médiane nulle reste écartée : ce n'est pas une donnée manquante mais un rapport indéfini.

**D-52 — La CI compile avec go1.27.0, pas avec la version minimale déclarée.** Le workflow n'avait jamais tourné : il vivait sous `EscapeBench/.github/`, que GitHub ne lit pas. Déplacé à la racine, il doit d'abord garder le code qui a produit les résultats archivés, et ceux-ci l'ont été sous `go1.27.0`. La version minimale de `go.mod` et de `CLAUDE.md`, Go 1.25, reste déclarée mais n'est pas exercée. Sa première exécution a d'ailleurs trouvé un défaut de LeakLab que le poste Windows ne pouvait pas voir.

**D-53 — La CI est retirée du dépôt, ce qui rend D-52 sans objet.** Le chercheur a demandé le retrait de toute CI/CD. Le dépôt n'a plus aucun pipeline : la vérification repose sur la commande de référence de chaque `CLAUDE.md`, exécutée à la main, et sur les hooks de l'agent. Ce qui disparaît avec elle est la seule vérification hors du poste Windows. Sa dernière exécution, sur le commit qui corrige `parseVet`, a passé les deux bancs sous `ubuntu-latest` et `windows-latest` avec `go1.27.0`. Les constats A-106 et A-107 de l'audit n'ont plus d'objet. Les passages qui citent la CI comme affirmation du livre (H-004 de LeakLab) ou comme acteur de spécification (`Pipeline CI`) restent inchangés, parce qu'ils décrivent le livre ou un usage prévu, pas une configuration du dépôt. Le commentaire du gabarit `go.mod.tmpl` reste lui aussi inchangé, puisque le modifier changerait l'empreinte du harnais.

**D-54 — L'historique public est réécrit pour en retirer les trois ouvrages sous droit d'auteur (2026-09-15).** Retirés de l'arbre, les PDF restaient accessibles par l'historique, ce qui contredisait le README (E-02 de l'évaluation académique). `git filter-branch` les retire de tous les commits de `main` et de la branche de la PR n° 1, sous leurs six chemins (racine et `Book/`) ; les trois commits qui ne faisaient qu'ajouter un PDF, et le commit `Books`, disparaissent. L'arbre de travail est inchangé, mais toutes les empreintes postérieures au 2026-09-08 changent : celles que citent `AUDIT.md`, `DECISION_LeakLab.md`, `RAPPORT-FINAL_LeakLab.md` et l'évaluation académique sont remplacées par leurs équivalentes. La référence `refs/pull/1/head` de GitHub, en lecture seule, garde les anciens commits tant que le support de GitHub ne l'a pas purgée.

**D-55 — La purge est achevée et l'historique sans rapport est retiré (2026-09-22, lot 0 du plan d'implantation de l'évaluation).** D-54 laissait les anciens objets dans trois refuges : les sauvegardes `refs/original/*` de `filter-branch`, le reflog et les paquets locaux (87 Mo), et, sur GitHub, la branche `claude/audit-md-implementation-cctjkk` et `refs/pull/1/head`. Sur décision du chercheur (Q1 du plan), une seconde réécriture retire en même temps les six commits sans rapport avec le projet : deux recherches sur les psaumes (2026-08-30 et 2026-08-31) et deux rapports sur l'entreprise agentique avec leurs figures (2026-09-06 et 2026-09-07). Ces fichiers n'étaient plus dans l'arbre depuis le 2026-09-07 : l'arbre de travail est identique octet pour octet, et l'historique passe de 67 à 61 commits. Toutes les empreintes changent de nouveau ; les dix citations d'`AUDIT.md`, `DECISION_LeakLab.md`, `RAPPORT-FINAL_LeakLab.md`, de l'évaluation et du plan sont remplacées par leurs équivalentes (appariement par date et message). Les commits `76d5c45` à `7314bfe` et `818b566` qu'évoque le plan d'implantation n'ont plus d'équivalent. Ensuite : sauvegardes et reflog supprimés, `gc --prune=now --aggressive` (paquet réduit à 2,4 Mo), aucun PDF atteignable depuis `main`; branche locale `audit-md` et branche distante supprimées; un seul `push --force` de `main`; les deux répertoires `.github/workflows/` vides (E-23) n'existent que dans la copie de travail du poste, puisque Git ne suit pas un répertoire vide : le dépôt publié ne les contient pas, et leur retrait local reste au chercheur. Reste hors de portée de l'agent : `refs/pull/1/head`, que seul le soutien de GitHub peut supprimer ; la demande et sa réponse sont à dater ici par le chercheur. L'état purgé porte l'étiquette `v1.0`. Un bundle de l'ancien historique, pris avant la réécriture, reste sur le poste du chercheur, hors dépôt.

*D-56 est réservée à la revue humaine des cas d'utilisation par un tiers (lot 1) ; elle sera écrite quand la revue aura eu lieu.*

**D-57 — L'état de l'art est écrit après les mesures, et il ne les a pas guidées (2026-09-22, lot 3 du plan d'implantation de l'évaluation).** [`ETAT-DE-L-ART.md`](ETAT-DE-L-ART.md) répond à E-01 douze jours après les campagnes d'EscapeBench et neuf jours après celles de LeakLab. Aucun critère gelé, verdict ou rationale n'en tient compte, et il ne s'en sert pas pour réinterpréter un verdict après coup. Ses références ont été retrouvées en source primaire par un agent (DOI résolu au registre Crossref, résumé ou texte lus selon le cas, niveau indiqué pour chacune). La lecture par le chercheur reste à faire, et les marqueurs *supposé* restent en place jusque-là (règle 6). Une référence du plan est retirée : la politique de badges d'artefact de l'ACM, dont la page a refusé l'accès (403). Trois résultats restent sans source retrouvée : l'effet mesuré de la forme de la structure sur la règle « valeur ou pointeur », la généralisation (k + 1)/k de H-010 et l'angle mort du profil `goroutineleak` sur un `sync.Mutex` alloué seul (LeakLab H-013). Consigné parce que l'ordre compte : écrit avant, cet état de l'art aurait changé le protocole sur six points. (1) **H-007 aurait exigé des réplicats dès sa rédaction.** Georges et coll. (2007) et Kalibera et Jones (2013) placent l'unité de répétition au niveau de l'exécution, pas de l'itération. Les 20 répétitions de chaque sujet sont des itérations dans un même processus, et un témoin nul à bras identiques ne mesure pas la variance d'une vraie paire entre processus. C'est le défaut que la revue contradictoire a trouvé ensuite et que H-012 a corrigé par cinq réplicats. H-001 aurait de même exigé un plancher de bruit : ses écarts de 0,05 et 0,06 ns sont du même ordre que ce plancher. (2) **L'ordre de mesure aurait été entrelacé et tiré au hasard** (Abedi et Brecht, 2017; Laaber et coll., 2019), au lieu de l'ordre fixe de la matrice où le bras valeur précède toujours le bras pointeur. (3) **La disposition en mémoire aurait été traitée comme un facteur de confusion** (Mytkowicz et coll., 2009; Curtsinger et Berger, 2013). L'avantage inexpliqué du pointeur sur la cellule de 8 octets à champ pointeur relève de cette classe de biais. (4) **H-004 aurait été écrite comme H-008** : la différence entre débit et latence, et la chaîne de pointeurs pour mesurer la latence, sont connues depuis *lmbench* (1996) et Drepper (2007). L'infirmation de H-004 porte sur une opérationnalisation que la littérature écartait déjà. (5) **LeakLab H-008 aurait été prédite.** L'*issue* golang/go n° 69188 (2024) signale que le délai de `go test` empêche la détection d'interblocage. Le banc aurait alors transmis ce délai à ses binaires de test dès la première campagne, et le verdict faux de `R-2026-09-13-1` aurait été évité. (6) **LeakLab H-002 et H-004 auraient été écrites comme confirmations attendues** de Tu et coll. (2019) et de Saioc et coll. (2024). `goleak` aurait pu servir de détecteur de référence, signalé hors bibliothèque standard. Aucun de ces points ne change un verdict publié : ils disent ce que le protocole aurait dû prévoir, et ils rejoignent les limites déjà écrites.

**D-58 — QR2 est mesurée après coup à partir des transcriptions locales : les agrégats et les prompts des revues sont publiés, les transcriptions ne le sont pas (2026-09-22, lot 4 du plan d'implantation de l'évaluation).** L'évaluation relevait que QR2 n'avait aucune mesure et que la méthode des revues par agents n'était pas décrite (E-05, E-04, E-27). Le script `Doc/outils/qr2-sessions.sh` (bash et `jq`) tire des 51 transcriptions locales et de leurs sous-agents des agrégats sans contenu : horodatages, tours, appels d'outils par type, sous-agents, jetons et modèle. `Doc/QR2-MESURES.md` les range par phase, nomme les trous et distingue ce qui est mesuré de ce qui est déduit. Les sessions postérieures au 2026-09-15 sont comptées à part, et les 38 sessions antérieures au 2026-09-08, qui portent sur d'autres travaux, sont exclues. `Revue/PROMPTS-REVUES_2026-09-10.md` archive, pour les six workflows du projet, le nombre d'agents, les rôles, le modèle, la règle d'accord et les gabarits de prompts tels qu'écrits dans les scripts, chemins locaux anonymisés. Ce qui n'est pas publié, sur décision du chercheur (Q4 du plan) : les transcriptions elles-mêmes, les réponses des sous-agents et tout autre texte de conversation. Elles mêlent au projet d'autres travaux et des données personnelles, et les prompts suffisent à rejouer la méthode. Le prix de ce choix : un tiers ne peut pas vérifier les agrégats sur la source ; il peut seulement rejouer le script sur ses propres transcriptions. La mesure a révélé quatre faits que le dépôt ne disait pas. Les « 172 agents » du rapport de campagne sont 9 auditeurs, 162 vérificateurs et 1 critique. L'audit du code a perdu 189 de ses 581 agents sur les limites d'usage du compte et n'a pas eu de seconde ronde. Son fichier d'exécution donne 119 constats confirmés sur 187, là où `Doc/AUDIT.md` en publie 102 sur 190. Enfin, aucun des six *skills* ni des deux sous-agents de revue du banc n'a été invoqué dans les sessions locales. Le travail des sessions en nuage du 2026-09-12 (implantation de l'audit, contre-vérification à huit agents) n'a aucune transcription locale et reste hors mesure.

**D-59 — La règle `synctest` est amendée plutôt qu'appliquée à la lettre, et le seul `time.Sleep` du banc disparaît quand même (2026-09-22, lot 5 du plan d'implantation de l'évaluation, E-12).** `CLAUDE.md` exigeait `testing/synctest` pour « tout comportement temporel ou concurrent ». Sur décision du chercheur (Q3), la règle vise désormais les délais, tickers ou goroutines *simulables* ; un test qui lit des compteurs réels du système (temps processeur de la machine ou d'un arbre de processus) en est exempté et le dit en commentaire. Le skill `/go-test` reprend la même formulation. Le plan prévoyait un commentaire d'exemption sur `internal/adapters/gotool/quietude_test.go`, ligne 96. À la relecture, ce test ne lit aucun compteur réel : exécuteur et sonde de quiétude y sont factices, et seule l'horloge murale de `Toolchain.Run` est réelle. L'exempter aurait contredit la règle qu'on venait d'écrire. Il tourne donc maintenant dans une bulle `synctest.Test` : le `time.Sleep` n'attend plus, la fenêtre vaut exactement 5 ms, et l'assertion, qui vérifiait seulement que la fraction était bornée, vérifie maintenant une valeur exacte (1, borne haute). L'exemption est déclarée sur le seul test qui en relève, `TestC010_SondeReelle` : il rapporte les compteurs réels à une durée réelle, et dans une bulle la fraction se déclarerait non mesurable. La même occasion ajoute à `CLAUDE.md` la convention de nommage des tests d'hypothèses (E-32) : `TestUC005_H###[_<slug>]` dans EscapeBench, `TestH###[_<slug>]` dans LeakLab, sans aucun renommage.

**D-60 — La Provenance consigne l'état de la machine, et chaque Measurement archive son `b.N` et sa sortie brute (2026-09-22, lot 6 du plan d'implantation de l'évaluation, E-14, E-19).** Quatre champs facultatifs entrent dans `Provenance` : `osVersion` (version, build et révision du système), `powerPlan` (plan d'alimentation actif sous Windows, nom et GUID ; gouverneur de fréquence sous Linux), `cpuAffinity` (masque du processus du banc, dont héritent les `go test`, ou « non épinglé ») et `coreTypes` (cœurs physiques de performance et d'efficacité, seulement quand la topologie les distingue). Ce sont les réglages qui déplacent un temps par opération sans changer ni la chaîne d'outils ni le processeur, les deux seuls que la Provenance identifiait. D'autres grandeurs ont été écartées : la fréquence instantanée, qui varie pendant la mesure et qu'un relevé ponctuel décrirait mal ; l'état du turbo, qu'aucune API standard n'expose sous Windows sans pilote ; la température ; et le curseur de mode d'alimentation de Windows 11, qui se superpose au plan. `powerPlan` est consigné et non imposé : le banc constate l'état de la machine, il ne la configure pas, et un banc qui changerait le plan d'alimentation mesurerait une machine que le chercheur n'a pas choisie. Contrairement à ce qu'annonçait le plan, `topology_*.go` ne relevait pas les types de cœurs : la détection est ajoutée (`GetLogicalProcessorInformationEx` et sa classe d'efficacité sous Windows, PMU `cpu_core`/`cpu_atom` sous Linux). Sur le poste de référence, elle donne 8 P + 16 E, « non épinglé », « Utilisation normale », Windows 10.0.26220.9492 (25H2). `Measurement` reçoit `iterations`, le `b.N` de chaque répétition (vide, ou exactement `count` valeurs strictement positives), et `rawOutputFile`. La sortie brute de `go test` est créée sous `results/campaigns/<id>/raw/` avant la Measurement qui en porte le chemin, de sorte qu'aucune Measurement ne désigne un fichier absent. Elle n'est jamais réécrite (BR-003-3) : une sortie laissée par une tentative tuée entre les deux écritures reste en place, et la reprise écrit la sienne sous un nom suffixé. Une Measurement déclassée en `FAILED` garde sa sortie brute. Aucun de ces champs n'entre dans une identité, une empreinte, la vérification de provenance d'une reprise ni un critère ; aucun gabarit n'est touché, et l'empreinte du harnais reste `551ce66b…`. `TestUC003_CampagnesArchiveesSansLesChampsDeD60` relit les dix campagnes archivées et leurs 2 003 Measurement sans ces champs. Une campagne de fumée hors dépôt (`--count 20`, quatre cellules) a produit des Measurement avec `iterations` et un fichier brut. LeakLab reçoit `osVersion` seulement (D-18 et suivantes de `DECISION_LeakLab.md`).

**D-61 — Contre-épreuves du lot 7 : les paramètres de `M-8f03757ac206` sont retrouvés, l'écart de la cellule écartée par H-012 est réduit sans être expliqué (2026-09-22, lot 7 du plan d'implantation de l'évaluation).** Contrairement à ce qu'affirmait D-42, les paramètres de `M-8f03757ac206` n'étaient pas perdus. `matrices/` est ignoré par Git, mais le `matrix.json` de la matrice est resté sur le poste. Relus et passés à `MatrixID()`, ils redonnent l'identifiant, 252 cellules et 4 sondes. Aucun balayage n'a donc été fait : une seule combinaison candidate, moins d'une seconde de calcul. Régénérée depuis ces paramètres sur une copie du module, la matrice reproduit octet pour octet les sources de sujets du poste. Il s'agit de cinq tailles de 8 à 1024 octets, cinq profils (`LOCAL` et les trois conteneurs, plus `RETURNED_ALLOCATING`), `NAMED_FIELDS` et `NAMED_FIELDS_SHAM`, des répétitions 1, 2, 4 et 16, des charges 1 et 2, et quatre sondes. Ils sont consignés dans `LANCEMENT.md` §5. Sur la même copie, le harnais de non-régression complété de ces paramètres rejoue 71 verdicts au lieu de 67 : les quatre verdicts de H-009 et H-010 sur `C-2026-09-10-2` et `C-2026-09-10-3` sont reproduits à l'identique, motif compris. Le harnais du dépôt reçoit cette cinquième entrée dans `archivedParameters()` (commit `UC-005:` du même lot) et rejoue désormais 71 verdicts, aucun sauté. Ce n'est qu'un test, et l'empreinte du harnais, qui ne hache que les gabarits, ne bouge pas. E-17 est clos. Le désassemblage de `Size0008FieldsPtr_LOCAL` (go1.27.0, les binaires de la campagne) confirme que le bras pointeur exécute un chargement de plus, mais ce chargement n'est pas la cause. La paire témoin `Size0008FieldsPlain_LOCAL` a la même structure et ne montre pas d'écart. Une mesure de contrôle hors campagne reproduit l'anomalie sur le seul bras `Size0008FieldsPtr_LOCAL_POINTER` (0,65 ns contre 0,88 à 0,93 pour les trois autres bras), et un décalage du code de 32 octets ne la change pas. Les seules différences de code machine avec le bras témoin sont l'ordre des emplacements de pile et un `NOP` de boucle. Le mécanisme (renommage mémoire, *supposé*) demande les compteurs de performance du processeur, hors du périmètre du banc. E-18 est réduit à ce reste. Le verdict de H-012 ne change pas : la contre-épreuve conforte la raison déclarée du retrait de la cellule. Rapport : `Campagnes/CONTRE-EPREUVE_H-012-8-octets.md`.

*D-62 est réservée au rejeu des campagnes sous Linux (lot 8), que lance le chercheur ; elle sera écrite avec leurs rapports.*

**D-63 — Le lot 9 de l'audit est appliqué sur une branche à part, `lot-9-arm64`, qui ouvre la série de comparaison arm64 sans encore la mesurer (2026-09-22, lot 9 du plan d'implantation de l'évaluation, E-03, E-08, E-09 ; solde D-50).** Le chercheur ne dispose d'aucune machine arm64 (Q6) : aucune campagne n'est lancée. La branche n'est pas fusionnée dans `main`, pour que les campagnes Linux du lot 8, lancées sur `main`, gardent l'empreinte `551ce66b…` et restent comparables aux campagnes publiées.

**Le lot 9 de l'audit, en une fois.** Pour A-246, la première voie de C-005 est prise : `harness.go` est embarqué et haché avec les gabarits, si bien qu'une modification de la dérivation des champs change désormais l'empreinte. Pour A-073, la garde `unsafe.Sizeof` devient bilatérale, sur les cellules comme sur les nœuds et les éléments des sondes. Dès sa première compilation, elle a trouvé un défaut réel. En disposition `ARRAY_FILL`, le type d'un mot déclarait `Fill [0]uint64` en dernier champ ; or le compilateur gc complète un champ final de taille nulle jusqu'au mot suivant. `Size0008Plain` et `Size0008Ptr` en `ARRAY_FILL` occupaient donc 16 octets et non 8 dans toute la série `551ce66b…`. Le gabarit omet désormais `Fill` à un mot ; les champs nommés n'étaient pas touchés. Pour A-081, la ligne de cache devient un paramètre de Matrix, `cacheLineBytes` : 64 par défaut ou 128, écrit dans l'identifiant hors défaut seulement, de sorte que les identifiants antérieurs ne bougent pas. C'est aussi un relevé facultatif de Provenance (sysfs sous Linux, `GetLogicalProcessorInformation` sous Windows, rien ailleurs). UC-003 refuse une matrice à sondes de parcours ou de chaîne dont la ligne diffère de celle relevée. La nouvelle empreinte est `fd4a470c1fea2dc1a366d5ffb26921cf5c14f8a40540c67a2fcf91a962d06100`, figée par `TestUC001_D63_EmpreinteDeLaSerieArm64` : toute retouche ultérieure du harnais appellera une nouvelle décision.

**Ce que cela change pour les campagnes archivées.** Rien pour les verdicts publiés. L'empreinte des critères (test D-47) ne porte que sur les hypothèses retenues par chaque campagne, dont le texte n'a pas bougé. UC-004 et UC-005 ne lisent pas l'empreinte du harnais, et le harnais de non-régression rejoue les 71 verdicts à l'identique. Sous le nouveau harnais, en revanche, ces campagnes ne peuvent plus être reprises (UC-003), leurs matrices ne peuvent plus être régénérées ni reclassées (UC-001, UC-002 : `ErrHarnessChanged`), et elles ne se comparent pas à la série arm64. Le défaut du type de 8 octets ne renverse aucun verdict : sans cette cellule, H-001 garde deux tailles infirmantes (16 et 24 octets) dans chaque campagne, et le point de bascule de H-002 reste inférieur ou égal à 24 octets. Mais les mentions « 8 o » de leurs rationales désignent un type de deux mots, ce qu'un erratum daté des rapports devra dire. L'effet sur les cellules ARRAY_FILL de 8 octets de H-003, H-006 et H-009 n'est pas vérifié ; il est jugé vraisemblablement nul.

**Successeurs.** H-014 et H-015 reprennent H-001 et H-002 sur cinq réplicats de processus séparés, contre un plancher et une barrière mesurés par cellule selon la construction de H-012 (E-08). C'est ce qu'exige C-011 : le réplicat se décline aussi sur `ARRAY_FILL` × `LOCAL`, et UC-003 refuse H-001 et H-002 sur une telle matrice, comme il refuse H-007 sur toute matrice à réplicats. H-016 reprend H-012 avec une règle de multiplicité explicite (E-09) : une cellule donne l'avantage au pointeur à quatre réplicats franchissants sur cinq, elle est indéterminée à trois, et un verdict qui dépend d'une cellule indéterminée est non concluant. La règle de trois de H-012 porterait le risque de fausse infirmation de la famille à 25,8 % si chaque réplicat franchissait à tort avec p = 0,2. Celle de quatre le tient à 3,3 %, et sous 5 % jusqu'à huit fois la valeur nominale de p. Les trois critères citent ce que la série `551ce66b…` a montré (préenregistrement séquentiel informé) et se déclarent non concluants sur toute campagne de cette empreinte. Ils ne sont gelés qu'à la première campagne qui les nomme, qui doit être la première campagne arm64 : matrice `M-ebbb95f809c8`, 162 sujets, avec H-012 (`LANCEMENT.md`, section « Série arm64 »).

**Branche** `lot-9-arm64`, sept commits, de `2c478cd` à `321301a`. Les trois lectures par agents de la section 6 du plan n'ont pas été faites.

**D-64 — Les notes de clôture des cinq UC renvoient à D-37, qui reprend ce qu'elles recopiaient (2026-09-22, lot 10 du plan d'implantation de l'évaluation, E-21).** D-37 disait que chaque fichier de UC consigne ce qui établit `Deployed` plutôt que de renvoyer à une autorité. Le paragraphe, identique à l'octet dans les cinq fichiers, est remplacé par une ligne qui renvoie à D-37 ; c'est donc ici qu'est conservé ce qu'il établissait. Le statut `Deployed` est défini par le tableau de bord comme « campagne exécutée et rapport publié ». Au 2026-09-10, il reposait sur : la suite complète au vert sous `-race -shuffle=on` ; une couverture de statements de 93,6 pour cent ; le contrôle des hooks et le contrôle des spécifications sans constatation ; une revue contradictoire de trente-cinq constats, suivie d'une rédaction contradictoire en deux passes, chaque constat et chaque version ayant été soumis à des vérificateurs chargés de les réfuter ; six campagnes menées de bout en bout, dont les rapports sont publiés à la racine du dépôt. `CLAUDE.md` réserve le passage `Reviewed → Approved` à une décision humaine ; l'agent l'a assumé sur mandat explicite (D-01). Ces faits sont repris tels qu'écrits le 2026-09-10, sans nouvelle vérification.

**D-65 — Les décomptes de la méthode de `AUDIT.md` sont corrigés par erratum, et l'état des 75 constats sans contre-vérification est établi contre le code (2026-09-22, lot 10, E-26).** Le recoupement du fichier d'exécution `wf_e30c209a-4c3` avec l'inventaire publié explique l'écart. D'abord, 190 entrées pour 187 constats, parce que A-128, A-133 et A-134 sont inscrits deux fois. Ensuite, 102 confirmés publiés contre 119 dans le fichier, parce que 17 confirmés du workflow ont été publiés comme non vérifiés. Enfin, des 84 non vérifiés, le fichier en réfute 62 par défaut, faute de réponse des vérificateurs sur les limites d'usage, et 2 par deux voix contre une (A-141, A-151). La cause probable, un rendu à partir d'un état du workflow antérieur à la dernière reprise, reste une supposition : l'état intermédiaire n'est pas conservé. Le paragraphe publié n'est pas réécrit (règle 5 du plan). Les 75 sont classés en trois valeurs d'après le code au 2026-09-22 : 19 implantés au fil de l'eau, 56 ouverts, aucun sans objet depuis D-53. Un constat fait à moitié est classé ouvert et marqué « en partie ». Aucun constat n'est retiré.

## 5. Ce qui est délibérément absent

Cette section datait de la fin de la construction. Elle est révisée à la clôture du 2026-09-10 : trois de ses quatre points sont levés.

- ~~Aucune campagne de référence n'a été exécutée.~~ **Levé.** Six campagnes ont été menées de bout en bout, dont la campagne de référence `C-2026-09-10-1` et une campagne à 541 sujets couvrant douze hypothèses. Les treize hypothèses du catalogue ont un verdict.
- **Colonne `Integration` du tableau de bord fondée sur la présence, non sur l'exécution.** Toujours assumé. Un test sous `//go:build integration_test` qui référence un cas d'utilisation suffit à marquer la colonne. Faire tourner la suite d'intégration à chaque régénération aurait rendu `escapebench dashboard` inutilisable au quotidien. Révisé le 2026-09-12 (D-41) : la contrainte de build se lit désormais au sens de `go/build/constraint`, et deux jeux de tests d'intégration réels remplissent la colonne.
- ~~`make` reste requis et absent du poste.~~ **Levé.** La commande de référence est désormais `go vet ./... && go test -race -shuffle=on -count=1 ./...`, dans `CLAUDE.md`, dans le skill `/implement` et dans `LANCEMENT.md`. Le `Makefile` reste disponible pour qui a `make` ; c'est ce que tout le projet a de toute façon utilisé.
- ~~Statut `Verified` non atteint.~~ **Levé.** Les cinq cas d'utilisation sont à `Deployed`, dernier statut de la chaîne, défini par le tableau de bord comme « campagne exécutée et rapport publié ». Chaque fichier de UC consigne ce qui l'établit.

## 6. Vérification

| Contrôle | Résultat |
|---|---|
| `go vet ./...`, `gofmt -l` | propre |
| `go test -race -shuffle=on -count=1 ./...` | tous les paquets au vert |
| Couverture de statements | **93,0 %** à la clôture (cible : 85 %) |
| `bash .claude/hooks/selftest.sh` | les quatre hooks passent, en chemins POSIX et Windows |
| Chaîne UC-001 → UC-005 sur une matrice réduite | matrice générée, échappement classé, campagne mesurée, comparaison calculée, verdicts produits, tableau de bord régénéré |

Couverture par paquet : `models` 97,8 % · `cli` 98,9 % · `dashboard` 98,1 % · `escape` 97,0 % · `gotool` 96,8 % · `specs` 94,4 % · `service` 94,2 % · `store` 87,9 % · `system` 85,2 % · `harness` 84,0 % · `cmd` 84,7 %. Le reste non couvert est constitué de branches d'erreur d'entrée-sortie non déclenchables sans injection de panne au niveau du système de fichiers.

**Deux défauts réels ont été trouvés par les tests pendant la construction**, tous deux corrigés : le classificateur attribuait `RETURN_POINTER` à un `return v` qui ne rend qu'une copie et descendait à tort dans le corps des closures ; le motif de lecture des exigences liées capturait `FR-002` à l'intérieur de `NFR-002`.
