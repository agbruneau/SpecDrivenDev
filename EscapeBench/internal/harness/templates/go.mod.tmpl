// Code généré par EscapeBench (UC-001) — ne pas modifier à la main.
// Une matrice est un module imbriqué (C-007) : ses sujets ne sont jamais compilés par
// `go vet ./...`, `go test ./...`, la CI ni les hooks du module principal.
module escapebench.local/matrix/{{.MatrixID}}

go 1.25
